{
  "//": "Hide the throwaway .configamatron test-env dir from the editor. It is gitignored and rebuilt from scratch each test run; if the editor sees it, the C# Dev Kit auto-builds the copied dns-responder .csproj and locks the folder, making the tests' rmSync fail with EPERM on Windows.",
  "files.exclude": {
    "**/.configamatron": true
  },
  "files.watcherExclude": {
    "**/.configamatron/**": true
  }
}
